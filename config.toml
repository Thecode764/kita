# The base URL of the site; the only required configuration variable.
base_url = "https://thecode764.github.io/kita/"

# The site title and description; used in feeds by default.
title = "کیتا 🇮🇷"
description = "یک تم زیبا و ساده برای بلاگ Zola."

# The default site author.
author = "آرتین کریمی"

# The default language; used in feeds.
default_language = "fa"

# When set to "true", a feed is automatically generated.
generate_feed = true

# The filename to use for the feed. Used as the template filename, too.
# Defaults to "atom.xml", which has a built-in template that renders an Atom 1.0 feed.
# There is also a built-in template "rss.xml" that renders an RSS 2.0 feed.
feed_filename = "atom.xml"

# The taxonomies to be rendered for the site and their configuration of the default languages
# Example:
#     taxonomies = [
#       {name = "تگ ها", feed = true}, # each tag will have its own feed
#       {name = "تگ ها"}, # you can have taxonomies with the same name in multiple languages
#       {name = "بخش ها", paginate_by = 5},  # 5 items per page for a term
#       {name = "نویسندگان"}, # Basic definition: no feed or pagination
#     ]
#
taxonomies = [{ name = "tags", rss = true, paginate_by = 5 }]

# Configuration of the Markdown rendering
[markdown]
# When set to "true", all code blocks are highlighted.
highlight_code = true

# A list of directories used to search for additional `.sublime-syntax` and `.tmTheme` files.
extra_syntaxes_and_themes = []

# The theme to use for code highlighting.
# See below for list of allowed values.
highlight_theme = "base16-ocean-dark"

# The Kita theme style config.
[extra]
# Enable comment support globally.
comment = true
# The URL of social image.
social_image = "icons/github.svg"

[extra.style]
# The custom background color.
# bg_color = ""

# The custom background color in dark mode.
# bg_dark_color = ""

# Enable header blur.
header_blur = false

# The custom header color, only available when `header_blur` is false.
header_color = "white"

# The custom header color in dark mode, only available when `header_blur` is false.
header_dark_color = "black"

# The profile on home page.
[extra.profile]
name = "کیتا - تم زولا"
bio = "کیتا یک تم زیبا و ساده برای بلاگ زولا. ترجمه شده به فارسی. پایدار با قابلیت های جدید"
# The URL of avatar.
avatar_url = "icons/github.svg"
# Invert color in dark mode.
avatar_invert = true

# The social icons below the profile on the home page.
# The `name` should be the file name of static/icons/*.svg or the icon name of https://simpleicons.org/
[[extra.profile.social]]
name = "github"
url = "https://github.com/Thecode764/kita"

[[extra.profile.social]]
name = "rss"
url = "$BASE_URL/atom.xml"

[[extra.profile.social]]
name = "discord"
url = "https://discord.com/users/1125429179685548112"

# The top menu.
[[extra.menu]]
name = "پروژه ها"
url = "$BASE_URL/projects"

[[extra.menu]]
name = "آرشیو"
url = "$BASE_URL/archive"

[[extra.menu]]
name = "تگ ها"
url = "$BASE_URL/tags"

[[extra.menu]]
name = "درباره ما"
url = "$BASE_URL/about"

[[extra.menu]]
name = "تست"
url = "$BASE_URL/test"

# The page footer options.
[extra.footer]
since = 2024
license_url = "https://creativecommons.org/licenses/by-sa/4.0/deed"

# The giscus comment options, only available when comment is enabled.
[extra.giscus]
repo = "Thecode764/kita"
repo_id = "R_kgDOMBFtNQ"
category = "General"
category_id = "DIC_kwDOMBFtNc4Cfp-K"
mapping = "pathname"
strict = 0
reactions_enabled = 1
emit_metadata = 0
input_position = "bottom"
lang = "fa"
loading = "lazy"

[extra.user]
website = "https://thecode764.github.io"

[extra.edit]
repo = "https://github.com/Thecode764/kita"
branch = "main"